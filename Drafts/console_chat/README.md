# Консольный чат — учебный проект (OTUS, C# Professional)

Архитектура консольного чата, который реализует общение нескольких пользователей через терминал.
Проект использует принципы **DDD**, **микросервисную** архитектуру, **многопоточность** и базу данных.
Стек: **C# / .NET**.

## Содержание

| Документ | Описание |
|----------|----------|
| [Функции и роли](./features_and_roles.md) | Список функциональности и ролей пользователей |
| [Архитектура (полная)](./architecture.md) | Полная архитектура: DDD, микросервисы, многопоточность, БД + UML |
| [Архитектура (MVP)](./architecture_mvp.md) | Минимальная версия, достаточная для базовых функций |
| [UML-диаграммы](./uml/) | Изображения диаграмм (классов, компонентов, последовательностей, деплоймента, ER) |
| [Схема взаимодействия](./uml/uml_interaction_flow.png) | Блок-схема потоков пользователя в чате |
| [Архитектура (блоки)](./uml/uml_architecture_blocks.png) | Блоки компонентов (БД, микросервисы) и стрелки взаимодействия |

## Ключевые решения

- **DDD** — доменная модель выделена в отдельные bounded-контексты (`Users`, `Chat`), агрегаты управляют инвариантами.
- **Микросервисы** — `ChatService` и `UserService` + `API Gateway`; между сервисами — синхронные HTTP-вызовы (без брокера сообщений).
- **Многопоточность** — асинхронная обработка команд, WebSocket-hub для push сообщений.
- **БД** — PostgreSQL, **единая общая база** для обоих сервисов; транзакции через Unit of Work, репозитории поверх EF Core.
- **Консольный клиент** — однопоточный UI на фоне асинхронного получения сообщений.

## Как читать

1. Прочитайте [функции и роли](./features_and_roles.md).
2. Изучите [полную архитектуру](./architecture.md) и UML-диаграммы в `./uml/`.
3. Сравните с [MVP-версией](./architecture_mvp.md), чтобы понять, что можно опустить на старте.
